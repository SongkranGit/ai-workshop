This backend uses Go + Fiber.

Setup:

1. Install Go (1.20+)
2. From this folder run:

   go get github.com/gofiber/fiber/v2
   go run main.go

API:

GET / -> returns "Hello, World from Fiber in backend!"
